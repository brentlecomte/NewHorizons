import { Observable } from 'rxjs';
export declare function cleantmp(options?: CleantmpOptions): Observable<string>;
export interface CleantmpOptions {
    prefix?: string;
    assets?: WebpackCompilationAssets;
    globToDisk?: string;
    globFromDisk?: string;
}
export interface WebpackCompilationAssets {
    [key: string]: {
        source: () => string;
        size: () => number;
    };
}
